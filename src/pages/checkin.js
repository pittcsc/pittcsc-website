import React, { useEffect } from "react";

const RedirectPage = ({ data }) => {
  useEffect(() => {
    window.location = "location of href"
  })
  return (
    <div>
    or build a custom sign up form here
    </div>
   )
})


export default RedirectPage;
